@page
@model LogoutModel
@{
    ViewData["Title"] = "Вихід з системи";
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Натисніть тут, щоб вийти з системи</button>
            </form>
        }
        else
        {
            <p>Ви успішно вийшли з програми.</p>
        }
    }
</header>
